# Exclude everything by default, then whitelist what the build actually needs.
**

# Go source + module
!platform/api/*.go
!platform/api/go.mod
!platform/api/go.sum
!platform/api/prompts/
!platform/api/prompts/**

# CSV/JSON data files the API reads at startup
!data/*.csv
!data/*.json
